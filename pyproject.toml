[tool.pytest.ini_options]
testpaths = ["backend/tests"]
python_files = ["test_*.py"]

[tool.ruff]
line-length = 100
target-version = "py311"
src = ["backend"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "ARG", "ASYNC", "C4", "N", "PERF", "RUF", "SIM"]
ignore = ["E501"]
